{
  "name": "rimlogging-bundle-worker",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.22.0",
    "@cloudflare/workers-types": "^5.20260908.1",
    "typescript": "^7.0.2",
    "vitest": "^4.1.7",
    "wrangler": "^4.130.0"
  },
  "overrides": {
    "sharp": ">=0.35.4"
  }
}
