name: weekly-verify

on:
  schedule:
    - cron: '0 13 * * 6'
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: release-${{ github.ref }}
  cancel-in-progress: false

jobs:
  verify:
    name: verify against current RimWorld, then republish
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
        with:
          persist-credentials: false

      - name: Setup .NET
        uses: actions/setup-dotnet@a98b56852c35b8e3190ac28c8c2271da59106c68 # v6
        with:
          dotnet-version: |
            8.0.x
            10.0.x

      - name: Setup Node
        uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7
        with:
          node-version: 22

      - name: Install release dependencies
        run: npm ci --ignore-scripts

      - name: dotnet restore
        run: dotnet restore RimWorks.RimLogging.slnx

      - name: Record resolved RimWorld version
        run: |
          version=$(dotnet list Source/RimWorks.RimLogging/RimWorks.RimLogging.csproj package \
            | awk '/Krafs.Rimworld.Ref/ {print $NF; exit}')
          echo "RIMWORLD_REF=${version:-unknown}" >> "$GITHUB_ENV"
          echo "resolved RimWorld ref: ${version:-unknown}"

      - name: dotnet build
        run: dotnet build RimWorks.RimLogging.slnx -c Release --nologo --no-restore

      - name: dotnet test
        run: |
          set -o pipefail
          dotnet test RimWorks.RimLogging.slnx -c Release --nologo --no-build \
            --logger "console;verbosity=normal" | tee test-output.txt
          passed=$(grep -oE 'Passed:[[:space:]]+[0-9]+' test-output.txt | grep -oE '[0-9]+' | tail -1)
          echo "TESTS_PASSED=${passed:-unknown}" >> "$GITHUB_ENV"

      - name: dotnet format
        run: dotnet format RimWorks.RimLogging.slnx --verify-no-changes

      - uses: RimWorks/mod-ci/.github/actions/steam-republish@d6261858b6b74c0cd3ca03c6402c954d58ac7fee # v1.5.1
        with:
          workshop-id: ${{ vars.WORKSHOP_ID }}
          steam-username: ${{ secrets.STEAM_USERNAME }}
          steam-config-vdf-b64: ${{ secrets.STEAM_CONFIG_VDF_B64 }}
          verified-rimworld: ${{ env.RIMWORLD_REF }}
          verified-tests: ${{ env.TESTS_PASSED }}
